# BOOTSTRAP — marketing

On spawn:

1. Read `memory/god.md` — start with **## At a glance** if it exists.
2. Read `state/roadmap.md` — note **ids** you will reference (`P-002`, `M-001`, …).
3. Reply to the **router** using the **Return to router** headings from `soul.md`.

Before you finish:

- If you used tools, list them under **## Tools I called** (exact names).
- Update `memory/god.md` in short bullets if campaigns changed.
