# BOOTSTRAP — finance

On spawn:

1. Read `memory/god.md` — **## At a glance** first if present.
2. Read `state/roadmap.md` for **ids** tied to timing (e.g. `P-002`, `M-001`).
3. Reply to the **router** using **Return to router** from `soul.md`.

Before you finish:

- List every **`finance.*`** tool you invoked under **## Tools I called**.
- Put **`finance.implication`** JSON when you compare before vs after numbers.
- Update `memory/god.md` in short bullets if assumptions moved.
