{
  "_comment_purpose": "Per-agent intent file. The LIVE settings the harness reads are composed by orchestrator/bootstrap.py into state/.openharness/settings.json (OPENHARNESS_CONFIG_DIR). Only the path_rules below are consumed by the orchestrator.",
  "_comment_role": "Finance. Reads roadmap; writes only to its own memory.",

  "permissions": {
    "path_rules": [
      { "pattern": "state/roadmap.*", "mode": "read" },
      { "pattern": "workspaces/finance/memory/**", "mode": "write" },
      { "pattern": "state/cascade-trace.jsonl", "mode": "append" }
    ]
  },

  "_tools_intent": "finance.* and roadmap.read_* from the mock MCP server. NOT product.* or marketing.*.",
  "_model_intent": "Inherits $GOONING_MODEL via AgentDefinition `model: inherit`."
}
