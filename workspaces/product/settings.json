{
  "_comment": "Workspace settings for product. Only this agent may write state/roadmap.*.",

  "model": {
    "_comment": "TODO(glue): set provider/model."
  },

  "permissions": {
    "path_rules": [
      { "pattern": "state/roadmap.*", "mode": "write" },
      { "pattern": "workspaces/product/memory/god.md", "mode": "write" },
      { "pattern": "state/cascade-trace.jsonl", "mode": "append" }
    ]
  },

  "tools": {
    "_comment": "TODO(glue): enable product.* and roadmap.* namespaces of the mock MCP server. Do NOT expose marketing.* or finance.*."
  }
}
